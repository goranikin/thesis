# Sync with data_generation/types/constants.py when changing defaults.
hydra:
  job:
    chdir: false

num_samples: 120000
batch_size: 12
seed: 1234
filename: null
min_nodes: 50
max_nodes: 50
solver_runtime: 2.0
depot_positioning: random
customer_positioning: random
demand_distribution: CV
