defaults:
  - data: tsp50
  - model: small
  - diffusion: categorical
  - training: default
  - inference: default
  - _self_

wandb:
  project: difusco-tsp
  mode: online
  run_name: null

data_path: ./data/tsp50_128000_concorde.txt
checkpoint_dir: ./checkpoints
checkpoint_tag: difusco_tsp

# Saves to checkpoints/difusco_tsp_YYYYMMDD_HHMMSS/{best,last}_model.pt
#
# uv run python -m difusco.tsp.main.run_train \\
#     training=remote \\
#     checkpoint_dir=/mount/checkpoints
